# Copy these into your local environment. Never commit a real API key.
OPENAI_API_KEY=your_api_key_here
SAARTHI_MODEL=gpt-5.6-luna
# Set to 1 if you want API errors to be shown instead of using the deterministic fallback.
SAARTHI_STRICT_AI=0
